
interface SettingsPageProps {
    // 必要なpropsがあれば追加
}

function SettingsPage(props: SettingsPageProps) {
    return (
        <div className="settings-page">
            <h2>設定</h2>
            <p>アプリケーション設定を実装予定です。</p>
        </div>
    );
}

export default SettingsPage; 